# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% (Rails.env.development? ? 25 : 0).times do |index| %>
task_<%= index %>:
  details: "Task #<%= index %>"
  created_at: <%= index.seconds.ago %>
  updated_at: <%= index.seconds.ago %>
<% end %>
