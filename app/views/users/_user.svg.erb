<svg class="h-8 w-8 bg-black text-white rounded-full">
  <text text-anchor="middle" x="50%" y="50%" dy="0.35em" fill="currentColor" font-family="Arial">
    <%= user.name.split.map(&:first).join %>
  </text>
</svg>
